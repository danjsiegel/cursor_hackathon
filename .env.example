# MiniMax M2.1 API (vision/reasoning)
# Get your key from https://platform.minimax.io (Account > API Keys)
MINIMAX_API_KEY=

# For native Text API (chat + image): use https://api.minimax.io
# Do NOT use .../anthropic – that is a different API; the key test and screenshot flow use the native endpoint above.
MINIMAX_BASE_URL=https://api.minimax.io

# Optional: use stub when no key (demo mode)
# USE_MINIMAX_STUB=true
